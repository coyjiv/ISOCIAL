import styled from "@emotion/styled";
import { Stack } from "@mui/material";

export const CardSkeletonWrapper = styled(Stack)({
  borderRadius: "8px",
  overflow: "hidden",
  width: "252px",
  height: "349px",
  boxShadow: "0 0 10px rgba(0, 0, 0, 0.1)",
});
