import styled from '@emotion/styled'
import { Stack } from '@mui/material'

export const PostFooterWrapper = styled(Stack)({
  padding: '4px 12px',
  width: '100%',
  gap: '4px',
})

export const PostActionsWrapper = styled(Stack)({
  flexDirection: 'row',
  gap: '4px',
})
