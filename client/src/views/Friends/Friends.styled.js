import styled from "@emotion/styled";
import { Stack } from "@mui/material";

export const FriendsPageWrapper = styled(Stack)({
  width: "100%",
  height: "calc(100vh - 54px)",
  flexDirection: "row",
});
